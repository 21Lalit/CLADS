services:
  - type: web
    name: clads
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: CLADS_MODE
        value: dry-run
      - key: CLADS_DB
        value: /tmp/clads.db

